[build]
  # 1. Netlify goes inside this folder
  base = "frontend"
  
  # 2. Netlify runs the build command here
  command = "npm run build"
  
  # 3. The output is just "dist" (relative to the base folder)
  publish = "dist"