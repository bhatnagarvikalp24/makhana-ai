# API Configuration
# For local development, use your laptop's IP address
# For production, use your Render backend URL

# DEVELOPMENT (Local Testing)
# Find your laptop's IP: ifconfig (Mac/Linux) or ipconfig (Windows)
# VITE_API_URL=http://192.168.1.100:8000

# PRODUCTION (Deployed Backend)
VITE_API_URL=https://your-backend.onrender.com

# Note:
# - localhost only works on the same device
# - Use your laptop's local IP (192.168.x.x) for mobile testing on same WiFi
# - Use Render URL for production/deployed frontend
